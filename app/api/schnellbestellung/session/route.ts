import { NextResponse } from "next/server";import { getSchnellSettings,SCHNELL_COOKIE,verifySessionToken } from "@/lib/server/schnellbestellung";import { readRequestCookie } from "@/lib/server/request-security";export async function GET(req:Request){const s=await getSchnellSettings();const p=verifySessionToken(readRequestCookie(req,SCHNELL_COOKIE),s);return NextResponse.json({ok:!!p,enabled:s.enabled,paused:s.paused,expiresAt:p?.exp||null,recheckRequired:p?Date.now()-Number(p.locAt)>s.recheckMinutes*60000:false,payments:{cash:s.cashEnabled,online:s.onlineEnabled,split:s.splitEnabled}},{headers:{"Cache-Control":"no-store"}})}