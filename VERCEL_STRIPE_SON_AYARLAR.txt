VERCEL + STRIPE SON AYARLAR
==========================

Vercel > Project > Settings > Environment Variables

1) STRIPE_SECRET_KEY
   Test: sk_test_...
   Canlı: sk_live_...

2) STRIPE_WEBHOOK_SECRET
   Stripe webhook oluşturulduktan sonra verilen whsec_...

3) NEXT_PUBLIC_BASE_URL
   https://www.burger-brothers.berlin

Webhook URL:
   https://www.burger-brothers.berlin/api/stripe/webhook

Eventler:
   checkout.session.completed
   checkout.session.async_payment_succeeded
   checkout.session.async_payment_failed
   checkout.session.expired

Sonra Vercel Redeploy.

Admin > Settings > Zahlungsarten:
   Online ödeme aktiv = AÇIK
   TV storno durumunda otomatik iade = AÇIK
   Split / Alman usulü ödeme aktiv = isteğe göre AÇIK
   Kişi başı servis ücreti = örn. 0,20 €
   Maksimum kişi = örn. 8

Güvenlik:
   sk_ ve whsec_ değerlerini GitHub'a koyma.
