name: BBSchnellApp
options:
  bundleIdPrefix: berlin.burgerbrothers
settings:
  base:
    IPHONEOS_DEPLOYMENT_TARGET: "16.0"
targets:
  BBSchnellApp:
    type: application
    platform: iOS
    sources:
      - Sources
      - Resources
    info:
      path: Config/Info.plist
    entitlements:
      path: Config/App.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: berlin.burgerbrothers.schnell
        PRODUCT_NAME: "BB Schnell"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""
